# Instruction for deployment with heroku

##### Add The Git Remote
  heroku git:remote -a ewebshop-api -r heroku

##### To set custom buildpack to heroku
  heroku buildpacks:set jincod/dotnetcore

##### Set Environment variable
  heroku config:set ASPNETCORE_ENVIRONMENT=Production

##### Finally, deployment
  git push heroku master


# Instruction to connect remote mongo server
>> mongo "mongodb+srv://bc-aymj0.gcp.mongodb.net/ewebshop"  --username arif
>> Enter Password: <type password>
>> <copy and paste the script>